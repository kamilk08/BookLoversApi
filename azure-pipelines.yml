# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [AzurePipelines (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration AzurePipelines --host AzurePipelines
#
# </auto-generated>
# ------------------------------------------------------------------------------

stages:
  - stage: windows_2019
    displayName: 'windows-2019'
    dependsOn: [  ]
    pool:
      vmImage: 'windows-2019'
    jobs:
      - job: Clean
        displayName: 'Clean'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Clean --skip'
      - job: Restore
        displayName: 'Restore'
        dependsOn: [  ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Restore --skip'
      - job: Compile
        displayName: 'Compile'
        dependsOn: [ Restore ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd Compile --skip'
      - job: ArchitectureTests
        displayName: 'ArchitectureTests'
        dependsOn: [ Compile ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd ArchitectureTests --skip'
      - job: UnitTests
        displayName: 'UnitTests'
        dependsOn: [ ArchitectureTests ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd UnitTests --skip'
      - job: PrepareInputFiles
        displayName: 'PrepareInputFiles'
        dependsOn: [ Clean ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd PrepareInputFiles --skip'
      - job: PrepareSqlServer
        displayName: 'PrepareSqlServer'
        dependsOn: [ PrepareInputFiles ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd PrepareSqlServer --skip'
      - job: CreateAuthContext
        displayName: 'CreateAuthContext'
        dependsOn: [ PrepareSqlServer ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreateAuthContext --skip'
      - job: CreateBookcaseContext
        displayName: 'CreateBookcaseContext'
        dependsOn: [ CreateAuthContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreateBookcaseContext --skip'
      - job: CreatePublicationsContext
        displayName: 'CreatePublicationsContext'
        dependsOn: [ CreateBookcaseContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreatePublicationsContext --skip'
      - job: CreateLibrariansContext
        displayName: 'CreateLibrariansContext'
        dependsOn: [ CreatePublicationsContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreateLibrariansContext --skip'
      - job: CreateRatingsContext
        displayName: 'CreateRatingsContext'
        dependsOn: [ CreateLibrariansContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreateRatingsContext --skip'
      - job: CreateReadersContext
        displayName: 'CreateReadersContext'
        dependsOn: [ CreateRatingsContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd CreateReadersContext --skip'
      - job: RunAuthIntegrationTests
        displayName: 'RunAuthIntegrationTests'
        dependsOn: [ CreateReadersContext ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd RunAuthIntegrationTests --skip'
      - job: RunLibrariansIntegrationTests
        displayName: 'RunLibrariansIntegrationTests'
        dependsOn: [ RunAuthIntegrationTests ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd RunLibrariansIntegrationTests --skip'
      - job: RunRatingsIntegrationTests
        displayName: 'RunRatingsIntegrationTests'
        dependsOn: [ RunLibrariansIntegrationTests ]
        steps:
          - task: Cache@2
            inputs:
              key: $(Agent.OS) | **/global.json, **/*.csproj
              restoreKeys: $(Agent.OS)
              path: ~/.nuget/packages
          - task: CmdLine@2
            inputs:
              script: './build.cmd RunRatingsIntegrationTests --skip'
